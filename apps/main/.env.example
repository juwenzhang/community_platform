# Luhanxin Community Platform — 主应用环境变量
#
# 开发环境：复制为 .env.local，留空即可（走 vite.config.ts 的 proxy）
# 生产环境：在 Vercel Dashboard → Settings → Environment Variables 中配置
#
# ⚠️ VITE_ 前缀的变量会被注入到前端 bundle，不要放真正的密钥！

# API 后端地址（Connect RPC baseUrl）
# 开发环境留空，走 vite dev server proxy → localhost:8000
# 生产环境设为后端域名，如 https://your-backend.hf.space
VITE_API_BASE_URL=
