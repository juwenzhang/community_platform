# PostgreSQL Client Authentication Configuration (HuggingFace Spaces)
# 单容器内所有连接来自 localhost，使用 trust（无密码）

# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
