# 2026-03-20 工作日志

## DevHub Community Platform 技术方案设计

- 完成 6 份技术方案文档，存放在 `docs/2026-03-20/` 目录下
- 文档状态：Draft — 等待用户 Review
- 用户明确要求：先 review 想法，确认后再说"可以落地 openspec 的相关文档"才开始执行 openspec

### 文档清单
1. `01-tech-overview.md` — 技术方案总览（架构图、技术栈、主题色、约定大于配置）
2. `02-frontend-architecture.md` — 前端架构（Garfish 微前端 + pnpm monorepo + React/Vue）
3. `03-backend-architecture.md` — 后端架构（Rust Axum 微服务 + SeaORM）
4. `04-database-design.md` — 数据库设计（PostgreSQL 完整表结构 + Redis 缓存策略）
5. `05-infrastructure.md` — 基础设施（Docker Compose + Prometheus/Grafana + 埋点 SDK + Sentry + CI/CD）
6. `06-feature-modules.md` — 功能模块规划（4 个开发阶段 + AI 扩展方向）

### 核心技术决策
- 微前端：Garfish (web-infra-dev)
- 主框架：React 18+ (核心) + Vue 3 (管理后台)
- 状态管理：Zustand (React) / Pinia (Vue)
- 后端：Rust + Axum + SeaORM + PostgreSQL + Redis
- 搜索：Meilisearch
- 监控：Prometheus + Grafana + Sentry + 自研埋点 SDK → ClickHouse
- 主题：天蓝色 (#0EA5E9) + 白色 / 深灰蓝暗色 (#0F172A)

---

## 项目重命名 + Protobuf 迁移 + 文档重组 + 技术文档 + 规范 Skill

### 变更概要
1. **项目重命名**：`devhub` → `luhanxin`（包名 `@luhanxin/*`、容器名 `luhanxin-*`、数据库名 `luhanxin_community`）
2. **Protobuf 替代 JSON**：前后端交互改用 Protobuf (prost + protobuf-es + buf)
3. **文档目录重组**：`docs/design/`(设计文档)、`docs/tech/`(技术文档)、`docs/logs/`(对话日志)
4. **技术文档**：
   - `docs/tech/01-data-serialization-formats.md` — Protobuf vs JSON vs 常见数据格式对比
   - `docs/tech/02-frontend-backend-protocols.md` — HTTP/1.1~HTTP/3, SSE, WebSocket, gRPC 协议对比
5. **项目规范 Skill**：创建 `.codebuddy/rules/luhanxin-community-platform-conventions.mdc`

### 核心技术决策更新
- 前后端 API 交互：HTTP/2 + Protobuf (Connect Protocol)
- 微服务间通信：gRPC + Protobuf (Tonic)
- WebSocket 通知：Protobuf 二进制消息
- 异步事件：Redis Streams + Protobuf 编码
- AI 流式输出：SSE (例外，可用 JSON)
- Proto 工具链：buf (lint + generate + breaking change 检测)

---

## 设计文档按日期组织

- 将 `docs/design/*.md` 移入 `docs/design/2026-03-20/` 按日期子目录组织
- 删除旧的 `docs/2026-03-20/`（未更新的原始版本），避免冗余
- 更新项目规范 `.codebuddy/rules/luhanxin-community-platform-conventions.mdc` 中的文档目录约定
- 最终结构：`docs/design/YYYY-MM-DD/` — 每次重大架构设计/变更创建日期目录

---

## OpenSpec: project-scaffolding change (4/4 artifacts 完成)

### 完成的 Artifacts
1. **proposal.md** — 项目骨架初始化提案（为什么做、做什么、5 个 capability）
2. **design.md** — 技术设计文档（6 个关键决策：buf/Gateway/Garfish/Workspace/shared-types/Docker）
3. **specs/** — 5 个 capability 规格说明：
   - `proto-schema/spec.md` — Protobuf 定义 + buf 代码生成管道
   - `backend-workspace/spec.md` — Rust Cargo workspace (gateway + svc-user + shared)
   - `frontend-monorepo/spec.md` — pnpm Monorepo (main + feed + shared-types + Biome)
   - `dev-environment/spec.md` — Docker Compose + .env + Makefile + .gitignore + README
   - `e2e-hello-world/spec.md` — 端到端 Connect Protocol 链路验证
4. **tasks.md** — 6 组共 35 个任务，按依赖顺序排列

### OpenSpec 配置更新
- 更新 `openspec/config.yaml`：添加项目上下文 + 自定义规则（中文撰写、Protobuf 优先、任务粒度 1-3h）

### 下一步
- 执行 tasks.md 中的任务，从 "1. 工程化基础配置" 开始
