# 2026-04-02 会话日志

## Session 1: main-state-management OpenSpec 创建

- 创建了 `openspec/changes/main-state-management/` 的全部 artifacts（design.md、specs/article-store/spec.md、specs/frontend-auth/spec.md、tasks.md）
- Proposal 已存在（之前创建）

## Session 2: main-state-management 实施

### 完成工作
实施文章状态管理集中化重构，将散落在 5 个组件中的 `createClient(ArticleService)` 和内联 RPC 调用收口到 `useArticleStore` Zustand store。

### 核心决策
- **单 Store 双 State**: `articles`（列表）+ `currentArticle`（详情）在同一个 store 中管理，通过独立字段隔离生命周期
- **articleClient 单例**: 仅在 store 模块顶层创建一次，所有 RPC 调用通过 store action 发起
- **渐进式替换**: 逐组件替换，保持 UI 不变

### 文件变更清单
| 文件 | 操作 | 说明 |
|------|------|------|
| `apps/main/src/stores/useArticleStore.ts` | modified | 从空壳实现为完整 store（6 个 action） |
| `apps/main/src/components/ArticleList/index.tsx` | modified | 消费 store 替代内联 RPC |
| `apps/main/src/pages/post/pages/detail/index.tsx` | modified | 消费 store 替代内联 RPC |
| `apps/main/src/pages/post/pages/edit/index.tsx` | modified | 消费 store 替代内联 RPC |
| `apps/main/src/pages/profile/pages/manage/index.tsx` | modified | 消费 store 替代内联 CRUD（瘦身 ~130 行） |
| `openspec/changes/main-state-management/design.md` | created | 设计文档 |
| `openspec/changes/main-state-management/specs/article-store/spec.md` | created | 文章 store 规格 |
| `openspec/changes/main-state-management/specs/frontend-auth/spec.md` | created | 认证 delta spec |
| `openspec/changes/main-state-management/tasks.md` | modified | 所有 6 个任务标记完成 |
