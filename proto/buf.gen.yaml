version: v2

clean: true

plugins:
  # Rust: prost → crates/shared/src/proto/
  - local: protoc-gen-prost
    out: ../crates/shared/src/proto
    opt:
      - compile_well_known_types=false

  # Rust: tonic (gRPC service stubs) → crates/shared/src/proto/
  - local: protoc-gen-tonic
    out: ../crates/shared/src/proto

  # TypeScript: protobuf-es → packages/shared-types/src/proto/
  - local: protoc-gen-es
    out: ../packages/shared-types/src/proto
    opt:
      - target=ts
